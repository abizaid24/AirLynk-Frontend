# Base URL of the AirLynk FastAPI backend (no trailing slash, no /api/v1 suffix)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Public URL this frontend will be deployed at (used for SEO metadata, sitemap, robots.txt)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
